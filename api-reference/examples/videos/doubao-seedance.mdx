---
title: '豆包 Seedance 视频生成示例'
description: '通过统一视频接口调用 Seedance 2.5，创建、查询并获取生成结果'
---

# 豆包 Seedance 视频生成

Seedance 2.5 支持文生视频、首帧/首尾帧图生视频，以及图片、视频和音频的多模态参考。任务采用异步模式：先创建任务，再使用任务 ID 查询结果。

## 模型与规格

| 模型 ID | 输出时长 | 输出分辨率 | 帧率 |
| --- | --- | --- | --- |
| `doubao-seedance-2-5-260628` | 4～30 秒 | 480p、720p | 24 fps |
| `doubao-seedance-2-0-260128` | 4～15 秒 | 480p、720p、1080p、4K | 24 fps |
| `doubao-seedance-2-0-fast-260128` | 4～15 秒 | 480p、720p | 24 fps |

Seedance 2.x 默认使用 720p。建议显式传入 `duration` 和 `resolution`，以便调用结果与费用预估保持一致。

## 创建任务

所有模型参数均位于请求顶层。下面的请求生成一段 5 秒、720p、带声音的视频，并要求在查询结果中返回尾帧图片。

```bash cURL
curl --request POST \
  --url https://api.tokenops.ai/v1/videos \
  --header 'Authorization: Bearer <API-KEY>' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "doubao-seedance-2-5-260628",
    "content": [
      {
        "type": "text",
        "text": "电影感航拍：清晨薄雾中的山谷，镜头平稳向前推进，远处阳光穿过云层"
      }
    ],
    "duration": 5,
    "resolution": "720p",
    "ratio": "16:9",
    "generate_audio": true,
    "return_last_frame": true,
    "watermark": false
  }'
```

创建成功后返回任务 ID：

```json
{ "id": "cgt-2026******-****" }
```

### 首尾帧图生视频

使用 `role` 区分首帧和尾帧。图片 URL 必须能够被模型服务访问。

```json
{
  "model": "doubao-seedance-2-5-260628",
  "content": [
    { "type": "text", "text": "从白天自然过渡到黄昏，保持主体一致，镜头缓慢拉远" },
    {
      "type": "image_url",
      "image_url": { "url": "https://example.com/first-frame.png" },
      "role": "first_frame"
    },
    {
      "type": "image_url",
      "image_url": { "url": "https://example.com/last-frame.png" },
      "role": "last_frame"
    }
  ],
  "duration": 6,
  "resolution": "720p",
  "generate_audio": true,
  "return_last_frame": true
}
```

### 多模态参考

参考素材使用 `reference_image`、`reference_video` 和 `reference_audio` 角色。提示词可用“图片 1”“视频 1”“音频 1”等自然语言指代对应素材。

```json
{
  "model": "doubao-seedance-2-5-260628",
  "content": [
    { "type": "text", "text": "保持图片 1 中人物外观，参考视频 1 的运镜，并按照音频 1 的节奏剪辑" },
    {
      "type": "image_url",
      "image_url": { "url": "https://example.com/character.png" },
      "role": "reference_image"
    },
    {
      "type": "video_url",
      "video_url": { "url": "https://example.com/camera-motion.mp4" },
      "role": "reference_video"
    },
    {
      "type": "audio_url",
      "audio_url": { "url": "https://example.com/rhythm.mp3" },
      "role": "reference_audio"
    }
  ],
  "duration": 10,
  "resolution": "720p",
  "ratio": "16:9",
  "generate_audio": true
}
```

Seedance 2.5 多模态参考最多支持 30 张图片；参考视频和参考音频各最多 10 个，单类素材总时长不超过 30 秒。具体素材格式、大小及组合限制以厂商最新规则为准。

## 查询任务

```bash cURL
curl --request GET \
  --url https://api.tokenops.ai/v1/videos/<TASK-ID> \
  --header 'Authorization: Bearer <API-KEY>'
```

统一接口状态包括 `in_queue`、`in_progress`、`completed`、`failed` 和 `expired`。成功响应示例：

```json
{
  "id": "cgt-2026******-****",
  "model": "doubao-seedance-2-5-260628",
  "status": "completed",
  "content": {
    "video_url": "https://example.com/generated-video.mp4",
    "last_frame_url": "https://example.com/generated-last-frame.png"
  },
  "seed": 42,
  "resolution": "720p",
  "ratio": "16:9",
  "duration": 5,
  "framespersecond": 24,
  "generate_audio": true,
  "service_tier": "default",
  "usage": { "completion_tokens": 108900, "total_tokens": 108900 }
}
```

`expired` 表示任务超过执行有效期，不能再继续处理，应重新创建任务。

## 常用参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `model` | string | 是 | 模型 ID，例如 `doubao-seedance-2-5-260628` |
| `content` | array | 是 | 文本及图片、视频、音频输入 |
| `duration` | integer | 否 | Seedance 2.5 为 4～30 秒 |
| `resolution` | string | 否 | Seedance 2.5 支持 `480p`、`720p`，默认 `720p` |
| `ratio` | string | 否 | 输出宽高比，例如 `16:9`、`9:16`、`1:1` 或 `adaptive` |
| `generate_audio` | boolean | 否 | 是否生成同步音频，默认由厂商模型决定 |
| `return_last_frame` | boolean | 否 | 是否在成功结果中返回尾帧图片 |
| `camerafixed` | boolean | 否 | 是否固定摄像机 |
| `service_tier` | string | 否 | 服务等级，透传给厂商校验 |
| `watermark` | boolean | 否 | 是否添加水印 |
| `seed` | integer | 否 | 随机种子；不传时由厂商生成 |

<Warning>
`camerafixed` 和 `service_tier` 会原样透传给厂商。经正式环境实测，Seedance 2.5 文生视频场景当前不接受这两个参数；是否支持应以具体模型、生成模式及厂商实际校验结果为准。
</Warning>

<Note>
查询接口返回的下载 URL 可能有有效期。任务成功后请及时下载并保存视频；应用侧轮询时建议采用递增间隔，避免高频请求。
</Note>
